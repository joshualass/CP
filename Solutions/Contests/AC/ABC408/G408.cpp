#include <bits/stdc++.h>
typedef long long ll;
typedef long double ld;
using namespace std;

/*
I read editorial and still not sure how to solve + how a lot of people are solving this.

Can first observe that for a minimum q, there is a unique p unless q = 1. 
The proof for this is pretty straightforward contradiction how if there are 2, then it is not the minimum. 


*/

void solve() {
    
    

}

signed main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int casi; cin >> casi;
    while(casi-->0) solve();

    return 0;
}