#include <bits/stdc++.h>
typedef long long ll;
typedef long double ld;
using namespace std;

signed main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    for(int i = 0; i < 51; i++) {
        string s; cin >> s;
        cout << "gen " << s << " > $\n";
    }

    return 0;
}
