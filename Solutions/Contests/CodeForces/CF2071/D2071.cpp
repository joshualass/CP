#include <bits/stdc++.h>
typedef long long ll;
typedef long double ld;
using namespace std;

void solve() {
    
    

}

signed main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    // int s; cin >> s;

    // vector<int> a(s + 1);
    // a[s] = 1;
    // for(int i = 1; i <= s; i++) {
    //     // cout << "i : " << i << " a[i] : " << a[i] << '\n';
    //     cout << a[i] << " ";
    // }
    // for(int i = s + 1; i <= 1000; i++) {
    //     int b = 0;
    //     for(int j = 1; j <= i / 2; j++) {
    //         b ^= a[j];
    //     }
    //     a.push_back(b);
    //     // cout << "i : " << i << " a[i] : " << a[i] << '\n';
    //     cout << a[i] << " ";
    // }


    int casi; cin >> casi;
    while(casi-->0) solve();

    return 0;
}