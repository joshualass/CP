#include <bits/stdc++.h>
typedef long long ll;
typedef long double ld;
using namespace std;

//andwerp carry, just dp[i][j][k][l], seeing if 2^i*3^j*5^k*7^l is possible. 

signed main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    

    return 0;
}