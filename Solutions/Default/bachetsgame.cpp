#include <bits/stdc++.h>
typedef long long ll;
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    freopen("input.dat","r",stdin);
    while(!cin.eof()) {
        int n, m; cin >> n >> m;
        
    }

    return 0;
}