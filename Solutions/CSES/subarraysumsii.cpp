#include <bits/stdc++.h>
typedef long long ll;
typedef long double ld;
using namespace std;
const ll MOD = 1e9 + 7;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    
    ll curr = 0;
    ll cnts = 0;
    unordered_map<ll,int> m;
    ll n, x; cin >> n >> x;
    for(int i = 0; i < n; i++) {
        ll num; cin >> num;
        if(m.find(x-num) != )
    }

    return 0;
}