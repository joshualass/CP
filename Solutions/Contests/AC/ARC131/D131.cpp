#include <bits/stdc++.h>
typedef long long ll;
typedef long double ld;
using namespace std;
const ll MOD = 1e9 + 7;

signed main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    
    ll n, m, d; cin >> n >> m >> d;

    vector<ll> ranges(m+1);
    for(auto &x: ranges) cin >> x;
    vector<ll> scores(m);
    for(auto &x: scores) cin >> x;
    


    return 0;
}