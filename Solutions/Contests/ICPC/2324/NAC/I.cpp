#include <bits/stdc++.h>
typedef long long ll;
typedef long double ld;
using namespace std;
const ll MOD = 1e9 + 7;

signed main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    
    int n, K; cin >> n >> K;

    int states = 0;

    
    for(int i = 0; i <= n; i++) {
        for(int j = 0; j < n * n; j++) {
            for(int k = 0; k < K; k++) {


                

            }
        }
    }

    

    cerr << "we made it!" << endl;

    return 0;
}