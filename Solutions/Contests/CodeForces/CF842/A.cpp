#include <bits/stdc++.h>
typedef long long ll;
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    int n;
    cin >> n;
    while (n-->0) {
        int num;
        cin >> num;
        cout << num - 1 << "\n";
    }
    return 0;
}