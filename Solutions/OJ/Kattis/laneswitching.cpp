#include <bits/stdc++.h>
typedef long long ll;
typedef long double ld;
using namespace std;
const ll MOD = 1e9 + 7;

signed main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    
    int n, m, r; cin >> n >> m >> r;
    vector<vector<pair<int,int>>> cars(n-1);
    for(int i = 0; i < m; i++) {
        int lane, length, start; cin >> lane >> length >> start;
        cars[lane].push_back({})
    }

    return 0;
}