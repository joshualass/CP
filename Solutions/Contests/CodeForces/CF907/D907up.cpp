#include <bits/stdc++.h>
typedef long long ll;
typedef long double ld;
typedef __int128_t lll;
using namespace std;
const ll MOD = 1e9 + 7;

void solve() {
    ll L, R; cin >> L >> R;

    lll l = L;
    lll r = R;



}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    vector<vector<ll>> ranges(0);

    for(int i = 2; i < 64; i++) {
        //i is the base or f(x)
        //now we just need to find the values where g(x) changes
        //numbers are bounded by 1 << i and (1 << (i + 1)) - 1

        

    }    

    int casi; cin >> casi;
    while(casi-->0) solve();

    return 0;
}